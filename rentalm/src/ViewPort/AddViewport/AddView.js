import React from 'react';
import PropCard from '../../Cards/PropCard'

const addView = (props) =>{

    return(
        <PropCard cardItems={props.cardItems}/>
    );
}
    
    
export default addView;